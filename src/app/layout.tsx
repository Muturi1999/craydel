import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Craydel Africa Travel - Unforgettable African Adventures",
  description: "Experience breathtaking safaris, serene beach escapes, and immersive cultural tours across Africa with Craydel Africa Travel.",
  keywords: "Africa travel, safari, tour packages, travel agency, Kenya tourism",
  openGraph: {
    title: "Craydel Africa Travel",
    description: "Feel The Wild. Live The Adventure.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
